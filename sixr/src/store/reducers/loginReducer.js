const initialtstate = {
    email:"",
    password:""
  };
  
  const loginReducer = (state = initialtstate, action) => {
    switch (action.type) {
      default:
        return state;
    }
  };
  
  export default loginReducer;