const initialState = {
    name:"",
}

const campaignFormReducer = (state = initialState, action) => {
    switch(action.type) {
        default :
        return state
    }
}

export default campaignFormReducer